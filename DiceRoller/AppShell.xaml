<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DiceRoller.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DiceRoller.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="DiceRoller">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:GameView}" 
        Route="MainPage" />
        <!--Muita atenção quando for mudar o nome da MainPage, verifivar se...-->
        <!-- o nome da página de DiceRoller está alterado para DiceRoller.Views-->

</Shell>
